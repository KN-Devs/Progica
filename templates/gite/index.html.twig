{% extends 'base.html.twig' %}

{% block title %}Gite{% endblock %}

{% block body %}
    <div class=" content flex-fill d-flex flex-row justify-content-center align-items-center m-20">
        <div class="gite_picture">  
            <img src="{{ gite.image}}" alt="">
        </div>
        <div class=" d-flex flex-co justify-content-center align-items-center p-10">
            <h1>{{ gite.nom }}</h1>
            <p>Surface: {{ gite.surface }}</p>
        </div>
    </div>
{% endblock %}
