<header class="d-flex flex-row-nowrap justify-content-center p-10">
    <div class="content flex-fill d-flex flex-row-nowrap align-items-center justify-content-between">
        <a class="header-logo" href="{{path('home')}}">
            <img src="{{asset('images/logo.png')}}" alt="Logo" >
        </a>
        <div>
            <button class="btn btn-primary mr-10">Inscription</button>
            <button class="btn btn-primary-reverse">Connexion</button>
        </div>
    </div>
</header>